package bioreactor;

/*
class ParamsOfInterest{

 Private List<Parameter>;

 ParamsOfInterest( List<parameter> parameters){
   this.parameters = parameters;
 }

 void getPrintable(){
   parameters.stream().forEach(Parameter::getValue);
 }
*
*
* */

public class Parameter {
}
